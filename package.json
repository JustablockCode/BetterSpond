{
  "name": "betterspond",
  "version": "0.0.1",
  "main": "src/main.js",
"scripts": {
  "start": "electron .",
  "build-win": "npx electron-packager . --platform win32 --arch x64 --out dist/win-x64 --overwrite && npx electron-packager . --platform win32 --arch ia32 --out dist/win-ia32 --overwrite && npx electron-builder --config ./electron-builder.json"
},
  "author": "justablock",
  "license": "UNLICENSED",
  "description": "A better Spond experience.",
  "dependencies": {
    "electron": "^36.2.0",
    "electron-builder": "^26.0.12",
    "electron-packager": "^17.1.2"
  }
}
